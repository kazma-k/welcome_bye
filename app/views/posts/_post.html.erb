
  <% if post.image.attached? %>
    <div class="card-deck">
      <div class="card">
        <%= image_tag post.image, alt: "top image", class: 'card-img-top width-100% height-200 '  %>
        <div class="card-body">
          <h4 class="card-title"><%= post.item_name %></h4>
          <p class="card-text"><%= post.text %></p>
          <p class="card-text"><small class="text-muted"><%= post.created_at %></small></p>
        </div>
      </div>
      <div class="card">
        <%= image_tag post.image, alt: "top image", class: 'card-img-top width-100% height-200 '  %>
        <div class="card-body">
          <h4 class="card-title"><%= post.item_name %></h4>
          <p class="card-text"><%= post.text %></p>
          <p class="card-text"><small class="text-muted"><%= post.created_at %></small></p>
        </div>
      </div>
      <div class="card">
        <%= image_tag post.image, alt: "top image", class: 'card-img-top width-100% height-200 '  %>
        <div class="card-body">
          <h4 class="card-title"><%= post.item_name %></h4>
          <p class="card-text"><%= post.text %></p>
          <p class="card-text"><small class="text-muted"><%= post.created_at %></small></p>
        </div>
      </div>
    </div>
  <% end %>
